<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >
    <Button
        android:id="@+id/activar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/activar"
        android:layout_marginBottom="5dp"/>
    
    <Button
        android:id="@+id/buscarSin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/activar"
        android:text="@string/buscarSin" 
        android:enabled="false"
        android:layout_marginBottom="5dp"/>
    
    <Button
        android:id="@+id/buscar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/buscarSin"
        android:text="@string/buscar" 
        android:enabled="false"
        android:layout_marginBottom="5dp"/>
    
    <Button
        android:id="@+id/procesar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/buscar"
        android:text="Procesar" 
        android:layout_marginBottom="5dp"/>
    <Button
        android:id="@+id/detener"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/procesar"
        android:text="Detener servicio" 
        android:layout_marginBottom="5dp"/>
	   
	<ListView
	    android:id="@+id/dispositivos"
	    android:layout_width="match_parent"
	    android:layout_height="wrap_content"
	    android:layout_alignParentBottom="true" 
	    android:layout_below="@+id/detener">
	</ListView>

</RelativeLayout>